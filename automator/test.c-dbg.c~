#include <stdio.h>
#include <stdio.h>

void main(int x, char **y) {
  int i;
  fprintf(stderr, "test.c:4:decl(int,i)\n");
  fprintf(stderr, "test.c:4:assign(int,i,%d)\n", i);
  fprintf(stderr, "test.c:5:scope_in\n");
  float ____0 = 0;
  fprintf(stderr, "test.c:5:decl(float,i)\n");
  fprintf(stderr, "test.c:5:assign(float,i,%f)\n", ____0);
  for (float i = ____0; i < 1; (i++) | (fprintf(stderr, "test.c:5:assign(float,i,%f)\n", i + 1) == 0))
  {
    i = 3;
    fprintf(stderr, "test.c:6:assign(float,i,%f)\n", i);
  }

  fprintf(stderr, "test.c:5:scope_out\n");
  i = 3;
  fprintf(stderr, "test.c:8:assign(float,i,%f)\n", i);
}
