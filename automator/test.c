#include <stdio.h>

void main(int x, char **y) {
    for (int i = 0; i < 10; i++) {
        switch(i) {
            case 1:
                if (1) {
                    int d = 1, e = 10, f;
                    d++;
                }
                break;
        }
        if (1) {
            int d = 1, e = 10, f;
            d += e;
        }
    }
}